SBC60 FROM 70 CLEANBASE

Base: SBC70 FROM 80 CLEANBASE that tested clean.

Changed only:
- BT_KEEPALIVE_SILENCE_IDLE_MS: 70 -> 60
- AUDIO_SLOT_COUNT_SBC: 11 -> 10

Kept unchanged:
- real SBC audio_timer_interval = 5
- SBC_LOW_DELAY_FRAMES_PER_PACKET = 4
- AAC/AAC-ELD/LDAC same as base
